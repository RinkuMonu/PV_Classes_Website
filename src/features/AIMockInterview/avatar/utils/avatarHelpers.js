export const parseVisemes = (audioData) => {
  // Future interface for lip-sync viseme processing
  return [];
};

export const mapStatusToAvatarState = (interviewStatus) => {
  // Maps standard PV interview status to Avatar State
  return 'IDLE';
};
